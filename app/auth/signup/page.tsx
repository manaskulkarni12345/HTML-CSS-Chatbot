'use client';

import SignupForm from '@/components/SignupForm'; // Change path based on where you save it

export default function SignupPage() {
  return (
    <SignupForm onSwitchToLogin={() => {}} />
  );
}
